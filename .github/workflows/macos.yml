name: macOS

on:
  push:
    branches: [feature/official-quota-sync-blue]
  pull_request:

jobs:
  test:
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v7
      - run: swift --version
      - run: swift test
